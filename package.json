{
  "name": "opencode-sync",
  "version": "0.1.0",
  "description": "Monorepo for OpenCode synchronization",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "bun run --filter ... build",
    "dev": "bun run --filter ... dev",
    "test": "bun test"
  }
}
